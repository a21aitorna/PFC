tags:
  - Password Recovery
summary: Verify if a username exists for password recovery
description:  First step in the password recovery process.  It verifies whether the given username exists or not
consumes:
  - application/json
parameters:
  - in: body
    name: body
    required: true
    schema:
      type: object
      required:
        - username
      properties:
        username:
          type: string
          example: "user123"
responses:
  200: 
    description: "User was found"
  400: 
    description: "Bad request — missing or invalid 'username' field"
  404: 
    description: "User was not found"
  500:
    description: Internal server error
